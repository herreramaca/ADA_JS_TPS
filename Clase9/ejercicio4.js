const prompt = require('prompt-sync')();
let nombre = prompt ("Ingrese su nombre: ");

if (nombre === "Hema") {console.log(`Su nombre es igual al programa`);
}
else {console.log(`Su nombre es difente al progrma.`);
}